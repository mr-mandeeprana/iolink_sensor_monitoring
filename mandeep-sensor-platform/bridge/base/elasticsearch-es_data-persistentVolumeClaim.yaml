#! elasticsearch-es_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: elasticsearch-es-data
    namespace: mandeep-sensor-platform
    labels:
        com.docker.compose.service: elasticsearch
        com.docker.compose.volume: es_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
